// `DisplayMetrics` crosses as the plain data a `Resources` reports, so what the
// class itself contributes is the density Android counts everything else
// against: the one a picture drawn at a scale of one is drawn at.
exports.DENSITY_DEFAULT = 160
